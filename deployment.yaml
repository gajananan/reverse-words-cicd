apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app: reverse-words
  name: reverse-words
spec:
  revisionHistoryLimit: 0
  template:
    spec:
      containers:
        - env:
            - name: RELEASE
              value: Stage Release
            - $patch: replace
          image: '"sha256:97832a95fb511288ca5180aa1e59ccd96746ca755a19f44360be2ddad4cde29b"'
          name: reverse-words
      imagePullSecrets:
        - name: mappregkey
