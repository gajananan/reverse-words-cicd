apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: reverse-words
  name: reverse-words
spec:
  template:
    spec:
      containers:
      - env:
        - name: RELEASE
          value: Stage Release
        - $patch: replace
        image: quay.io/gajananan/tekton-reversewords:352a6e7de65d61bdc71a11a827aa7d0f55b08609
        name: reverse-words
      imagePullSecrets:
      - name: registry-secret
