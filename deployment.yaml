apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: reverse-words
  name: reverse-words
spec:
  template:
    spec:
      containers:
      - env:
        - name: RELEASE
          value: Stage Release
        - $patch: replace
        image: quay.io/gajananan/tekton-reversewords:7a536907ec702c9c57dc33dff537a692d9bb42bf
        name: reverse-words
      imagePullSecrets:
      - name: registry-secret
