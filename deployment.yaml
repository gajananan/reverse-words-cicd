apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app: reverse-words
  name: reverse-words
spec:
  revisionHistoryLimit: 0
  template:
    spec:
      containers:
      - env:
        - name: RELEASE
          value: Stage Release
        - $patch: replace
        image: us.icr.io/mutation-advisor/tekton-reversewords:76c1cfb93eb5e524b6948d49c5616b12f080a6d4
        name: reverse-words
      imagePullSecrets:
      - name: registry-secret
