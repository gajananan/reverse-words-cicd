bases:
  - github.com/gajananan/reverse-words-cicd?ref=config
commonLabels:
  app: reversewords-stage
namespace: reverse-words-stage
patchesStrategicMerge:
  - deployment.yaml
resources:
  - namespace.yaml
  - ingress.yaml
  - rsp.yaml
  - rs.yaml
  - image_policy.yaml
  - sealed-mappregkey.yaml
  - sealed-image-keyring-secrey.yaml
